import SwiftUI

struct BoardView: View {
    var body: some View {
        ZStack {
            Color.blue
            Text("Экран доски")
                .foregroundColor(.white)
                .font(.title)
        }
    }
}
