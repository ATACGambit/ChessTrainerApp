import SwiftUI

struct LessonsView: View {
    var body: some View {
        ZStack {
            Color.green
                .ignoresSafeArea()
            Text("Экран Уроки")
                .font(.largeTitle)
                .foregroundColor(.white)
        }
    }
}
